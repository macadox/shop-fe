export type ProductIdType = number | string;

export type ProductWidgetType = {
  id: ProductIdType;
  name: string;
  price: number;
  src: string;
  favorited: boolean;
};
// type ProductWidget = {};
